# Surcharges dev: le reste est dans application.properties

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.root=INFO
logging.level.com.devikapps.caverne=DEBUG
logging.level.org.springframework.web=INFO

# Local JWT — dev only fallback (override via AUTH_LOCAL_JWT_SECRET in real environments)
auth.local.jwt.secret=${AUTH_LOCAL_JWT_SECRET:dev-only-change-me-please-use-a-32-byte-secret}
