openapi: 3.0.3
info:
  title: La Caverne Malgache API
  description: |
    Documentation OpenAPI de La Caverne Malagasy.
    Couvre l'ensemble des ressources : utilisateurs, catalogue, commandes, paiements, livraisons.
  version: 1.0.0
  contact:
    name: Mr
servers:
  - url: https://api.lacaverne/v1
    description: Serveur de production

# ─────────────────────────────────────────────
# SECURITY
# ─────────────────────────────────────────────
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  # ─── SCHEMAS ──────────────────────────────
  schemas:

    # ── AUTH ──
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password

    LoginResponse:
      type: object
      properties:
        access_token:
          type: string
        token_type:
          type: string
          example: Bearer
        expires_in:
          type: integer
          example: 3600

    RegisterRequest:
      type: object
      required: [firstname, lastname, email, password]
      properties:
        firstname:
          type: string
          maxLength: 100
        lastname:
          type: string
          maxLength: 50
        email:
          type: string
          format: email
        phone:
          type: string
          maxLength: 20
        password:
          type: string
          format: password

    # ── USER ──
    User:
      type: object
      properties:
        id:
          type: integer
        firstname:
          type: string
        lastname:
          type: string
        email:
          type: string
          format: email
        phone:
          type: string
        role:
          type: string
          enum: [admin, simple_user]
        status:
          type: string

    UserUpdate:
      type: object
      properties:
        firstname:
          type: string
        lastname:
          type: string
        phone:
          type: string

    # ── ADDRESS ──
    Address:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        location:
          type: string
        postal_code:
          type: string
        country_code:
          type: string
          maxLength: 3
        is_default:
          type: boolean
          default: false

    AddressInput:
      type: object
      required: [location, postal_code, country_code]
      properties:
        location:
          type: string
        postal_code:
          type: string
        country_code:
          type: string
          maxLength: 3
        is_default:
          type: boolean

    RecipientInput:
      type: object
      required: [location, postal_code, country_code, recipient_name, recipient_email, recipient_phone]
      properties:
        location:
          type: string
        postal_code:
          type: string
        country_code:
          type: string
          maxLength: 3
        recipient_name:
          type: string
        recipient_email:
          type: string
          format: email
        recipient_phone:
          type: string

    # ── CATEGORY ──
    Category:
      type: object
      properties:
        id:
          type: integer
        label:
          type: string
        slug:
          type: string
        map:
          type: string
          maxLength: 100
        parent_id:
          type: integer
          nullable: true
        children:
          type: array
          items:
            $ref: '#/components/schemas/Category'

    CategoryInput:
      type: object
      required: [label, slug]
      properties:
        id:
          type: integer
          nullable: true
        label:
          type: string
        slug:
          type: string
        map:
          type: string
        parent_id:
          type: integer
          nullable: true

    # ── CURRENCY ──
    Currency:
      type: object
      properties:
        code_iso:
          type: string
          maxLength: 3
          example: MGA
        name:
          type: string
        symbol:
          type: string

    # ── PRODUCT ──
    Product:
      type: object
      properties:
        id:
          type: integer
        category_id:
          type: integer
        label:
          type: string
          maxLength: 255
        reference:
          type: string
          maxLength: 100
        limit_date:
          type: string
          format: date
          nullable: true
        description:
          type: string
        size:
          type: string
        is_active:
          type: boolean
          default: true
        images:
          type: array
          items:
            $ref: '#/components/schemas/ProductImage'
        prices:
          type: array
          items:
            $ref: '#/components/schemas/Price'
        technical_specs:
          $ref: '#/components/schemas/TechnicalSpecs'

    ProductInput:
      type: object
      required: [category_id, label, reference]
      properties:
        category_id:
          type: integer
        label:
          type: string
        reference:
          type: string
        limit_date:
          type: string
          format: date
        description:
          type: string
        size:
          type: string
        is_active:
          type: boolean

    ProductImage:
      type: object
      properties:
        id:
          type: integer
        product_id:
          type: integer
        url:
          type: string
          format: uri
        is_main:
          type: boolean
          default: false

    ProductImageInput:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
        is_main:
          type: boolean

    # ── PRICE ──
    Price:
      type: object
      properties:
        id:
          type: integer
        product_id:
          type: integer
        currency_code:
          type: string
          maxLength: 3
        value:
          type: number
          format: double
        valid_from:
          type: string
          format: date
        unit:
          type: string

    PriceInput:
      type: object
      required: [currency_code, value, valid_from]
      properties:
        currency_code:
          type: string
        value:
          type: number
          format: double
        valid_from:
          type: string
          format: date
        unit:
          type: string

    # ── STOCK ──
    StockEntry:
      type: object
      properties:
        id:
          type: integer
        product_id:
          type: integer
        quantity:
          type: number
          format: double
        movement_type:
          type: string
          enum: [in, out, adjustment]
        date:
          type: string
          format: date-time
        unit:
          type: string

    StockInput:
      type: object
      required: [quantity, movement_type]
      properties:
        quantity:
          type: number
          format: double
        movement_type:
          type: string
          enum: [in, out, adjustment]
        unit:
          type: string

    StockSummary:
      type: object
      properties:
        product_id:
          type: integer
        available_quantity:
          type: number
        unit:
          type: string
        last_updated:
          type: string
          format: date-time

    # ── TECHNICAL SPECS ──
    TechnicalSpecs:
      type: object
      properties:
        id:
          type: integer
        product_id:
          type: integer
        country:
          type: string
        region:
          type: string
        climate:
          type: string
        provider:
          type: string
          maxLength: 255

    TechnicalSpecsInput:
      type: object
      properties:
        country:
          type: string
        region:
          type: string
        climate:
          type: string
        provider:
          type: string

    # ── REVIEW ──
    Review:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        product_id:
          type: integer
        score:
          type: integer
          minimum: 1
          maximum: 5
        comment:
          type: string
        created_at:
          type: string
          format: date-time

    ReviewInput:
      type: object
      required: [score]
      properties:
        score:
          type: integer
          minimum: 1
          maximum: 5
        comment:
          type: string

    # ── ORDER ──
    OrderItem:
      type: object
      properties:
        product_id:
          type: integer
        quantity:
          type: number
          format: double
        unit_price:
          type: number
          format: double

    Order:
      type: object
      properties:
        id:
          type: integer
        user_id:
          type: integer
        address_id:
          type: integer
        currency_code:
          type: string
        reference:
          type: string
        date:
          type: string
          format: date-time
        status:
          type: string
          enum: [pending, confirmed, delivered, cancelled]
        delivery_cost_id:
          type: integer
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrderItem'
        recipient:
          $ref: '#/components/schemas/RecipientInput'
        provider_response:
          type: object
          nullable: true

    OrderInput:
      type: object
      required: [currency_code, items, recipient]
      properties:
        address_id:
          type: integer
          nullable: true
        delivery_cost_id:
          type: integer
          nullable: true
        currency_code:
          type: string
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [product_id, quantity]
            properties:
              product_id:
                type: integer
              quantity:
                type: number
                format: double
        recipient:
          $ref: '#/components/schemas/RecipientInput'

    OrderStatusUpdate:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [pending, confirmed, delivered, cancelled]

    # ── DELIVER COST ──
    DeliverCost:
      type: object
      properties:
        id:
          type: integer
        order_id:
          type: integer
        amount:
          type: number
          format: double
        provider:
          type: string
        response_provider:
          type: object
          nullable: true

    DeliverCostInput:
      type: object
      required: [amount, provider]
      properties:
        amount:
          type: number
          format: double
        provider:
          type: string

    # ── PAYMENT METHOD ──
    PaymentMethod:
      type: object
      properties:
        provider_code:
          type: string
        label:
          type: string
          maxLength: 100
        is_active:
          type: boolean
        requires_manual_check:
          type: boolean
        integration_type:
          type: string
          enum: [manual, api, webhook]

    PaymentMethodInput:
      type: object
      required: [provider_code, label, integration_type]
      properties:
        provider_code:
          type: string
        label:
          type: string
        is_active:
          type: boolean
        requires_manual_check:
          type: boolean
        integration_type:
          type: string
          enum: [manual, api, webhook]

    # ── PAYMENT ──
    Payment:
      type: object
      properties:
        id:
          type: integer
        order_id:
          type: integer
        method_code:
          type: string
        currency_code:
          type: string
        amount:
          type: number
          format: double
        date:
          type: string
          format: date-time
        status:
          type: string
          enum: [pending, confirmed, failed, refunded]
        internal_reference:
          type: string
        provider_response:
          type: object
          nullable: true

    PaymentInput:
      type: object
      required: [method_code, currency_code, amount]
      properties:
        method_code:
          type: string
        currency_code:
          type: string
        amount:
          type: number
          format: double

    # ── COMMON ──
    PaginatedMeta:
      type: object
      properties:
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer
        last_page:
          type: integer

    Error:
      type: object
      properties:
        code:
          type: integer
        message:
          type: string

  # ─── RESPONSES ────────────────────────────
  responses:
    Unauthorized:
      description: Token JWT manquant ou invalide
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Forbidden:
      description: Accès refusé (rôle insuffisant)
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: Ressource introuvable
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    UnprocessableEntity:
      description: Données invalides
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  # ─── PARAMETERS ───────────────────────────
  parameters:
    pathId:
      name: id
      in: path
      required: true
      schema:
        type: integer
    queryPage:
      name: page
      in: query
      schema:
        type: integer
        default: 1
    queryPerPage:
      name: per_page
      in: query
      schema:
        type: integer
        default: 20

security:
  - BearerAuth: []

# ─────────────────────────────────────────────
# PATHS
# ─────────────────────────────────────────────
paths:

  # ═══════════════════════════
  # AUTH
  # ═══════════════════════════
  /auth/register:
    post:
      tags: [Auth]
      summary: Inscription d'un nouvel utilisateur
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: Utilisateur créé
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /auth/login:
    post:
      tags: [Auth]
      summary: Connexion — obtenir un JWT
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Token JWT retourné
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /auth/logout:
    post:
      tags: [Auth]
      summary: Invalidation du token JWT courant
      responses:
        '204':
          description: Déconnexion réussie
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ═══════════════════════════
  # USERS
  # ═══════════════════════════
  /users:
    get:
      tags: [Users]
      summary: Liste des utilisateurs (admin)
      parameters:
        - $ref: '#/components/parameters/queryPage'
        - $ref: '#/components/parameters/queryPerPage'
        - name: role
          in: query
          schema:
            type: string
            enum: [admin, simple_user]
      responses:
        '200':
          description: Liste paginée
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/User'
                  meta:
                    $ref: '#/components/schemas/PaginatedMeta'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /users/me:
    get:
      tags: [Users]
      summary: Profil de l'utilisateur connecté
      responses:
        '200':
          description: Profil retourné
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/Unauthorized'
    put:
      tags: [Users]
      summary: Mise à jour du profil
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpdate'
      responses:
        '200':
          description: Profil mis à jour
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /users/{id}:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Users]
      summary: Détail d'un utilisateur (admin)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Users]
      summary: Suppression d'un utilisateur (admin)
      responses:
        '204':
          description: Supprimé
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  # ═══════════════════════════
  # ADDRESSES
  # ═══════════════════════════
  /users/me/addresses:
    get:
      tags: [Addresses]
      summary: Liste des adresses de l'utilisateur connecté
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Address'
    post:
      tags: [Addresses]
      summary: Ajouter une adresse
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddressInput'
      responses:
        '201':
          description: Adresse créée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'

  /users/me/addresses/{id}:
    parameters:
      - $ref: '#/components/parameters/pathId'
    put:
      tags: [Addresses]
      summary: Modifier une adresse
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddressInput'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Addresses]
      summary: Supprimer une adresse
      responses:
        '204':
          description: Supprimée
        '404':
          $ref: '#/components/responses/NotFound'

  /users/me/addresses/{id}/default:
    parameters:
      - $ref: '#/components/parameters/pathId'
    put:
      tags: [Addresses]
      summary: Définir comme adresse par défaut
      responses:
        '200':
          description: Adresse définie par défaut
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Address'

  # ═══════════════════════════
  # CATEGORIES
  # ═══════════════════════════
  /categories:
    get:
      tags: [Categories]
      summary: Liste des catégories (arbre)
      security: []
      parameters:
        - name: flat
          in: query
          description: Si true, retourne une liste plate sans enfants
          schema:
            type: boolean
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Category'
    post:
      tags: [Categories]
      summary: Créer ou mettre à jour une catégorie (admin)
      description: >
        Endpoint d'upsert. Si `id` est absent ou null dans le payload, une nouvelle catégorie est créée.
        Si `id` est fourni, la catégorie existante correspondante doit être mise à jour.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoryInput'
      responses:
        '201':
          description: Créée quand `id` est absent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '200':
          description: Mise à jour quand `id` est fourni
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'

  /categories/{id}:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Categories]
      summary: Détail d'une catégorie
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Categories]
      summary: Créer ou mettre à jour une catégorie par identifiant (admin)
      description: >
        Endpoint d'upsert avec identifiant dans l'URL. Le `id` du path fait foi.
        Si `id` est aussi fourni dans le payload, il doit correspondre à l'identifiant du path.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoryInput'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '201':
          description: Créée si l'identifiant du path n'existe pas encore et que l'implémentation choisit de créer
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
    delete:
      tags: [Categories]
      summary: Supprimer une catégorie (admin)
      responses:
        '204':
          description: Supprimée

  # ═══════════════════════════
  # PRODUCTS
  # ═══════════════════════════
  /products:
    get:
      tags: [Products]
      summary: Catalogue produits
      security: []
      parameters:
        - $ref: '#/components/parameters/queryPage'
        - $ref: '#/components/parameters/queryPerPage'
        - name: category_id
          in: query
          schema:
            type: integer
        - name: is_active
          in: query
          schema:
            type: boolean
        - name: search
          in: query
          schema:
            type: string
        - name: currency
          in: query
          schema:
            type: string
            example: MGA
      responses:
        '200':
          description: Liste paginée
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Product'
                  meta:
                    $ref: '#/components/schemas/PaginatedMeta'
    post:
      tags: [Products]
      summary: Créer un produit (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductInput'
      responses:
        '201':
          description: Produit créé
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'

  /products/{id}:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Products]
      summary: Détail d'un produit
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Products]
      summary: Modifier un produit (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductInput'
      responses:
        '200':
          description: Mis à jour
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
    delete:
      tags: [Products]
      summary: Supprimer un produit (admin)
      responses:
        '204':
          description: Supprimé

  /products/{id}/images:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Products]
      summary: Images d'un produit
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProductImage'
    post:
      tags: [Products]
      summary: Ajouter une image (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductImageInput'
      responses:
        '201':
          description: Créée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductImage'

  /products/{id}/images/{imageId}:
    parameters:
      - $ref: '#/components/parameters/pathId'
      - name: imageId
        in: path
        required: true
        schema:
          type: integer
    put:
      tags: [Products]
      summary: Modifier une image (définir main, etc.)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductImageInput'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductImage'
    delete:
      tags: [Products]
      summary: Supprimer une image
      responses:
        '204':
          description: Supprimée

  /products/{id}/prices:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Products]
      summary: Historique des prix d'un produit
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Price'
    post:
      tags: [Products]
      summary: Ajouter un prix (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PriceInput'
      responses:
        '201':
          description: Créé
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Price'

  /products/{id}/stock:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Products]
      summary: Stock disponible d'un produit
      responses:
        '200':
          description: Résumé du stock
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockSummary'
    post:
      tags: [Products]
      summary: Enregistrer un mouvement de stock (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StockInput'
      responses:
        '201':
          description: Mouvement enregistré
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StockEntry'

  /products/{id}/stock/history:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Products]
      summary: Historique des mouvements de stock
      parameters:
        - $ref: '#/components/parameters/queryPage'
        - name: movement_type
          in: query
          schema:
            type: string
            enum: [in, out, adjustment]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/StockEntry'
                  meta:
                    $ref: '#/components/schemas/PaginatedMeta'

  /products/{id}/technical-specs:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Products]
      summary: Spécifications techniques d'un produit
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TechnicalSpecs'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Products]
      summary: Créer ou remplacer les specs techniques (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TechnicalSpecsInput'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TechnicalSpecs'

  /products/{id}/reviews:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Products]
      summary: Avis clients d'un produit
      security: []
      parameters:
        - $ref: '#/components/parameters/queryPage'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  average_score:
                    type: number
                    format: float
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Review'
                  meta:
                    $ref: '#/components/schemas/PaginatedMeta'
    post:
      tags: [Products]
      summary: Déposer un avis (utilisateur connecté)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReviewInput'
      responses:
        '201':
          description: Avis enregistré
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Review'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  # ═══════════════════════════
  # ORDERS
  # ═══════════════════════════
  /orders:
    get:
      tags: [Orders]
      summary: Mes commandes (utilisateur connecté)
      parameters:
        - $ref: '#/components/parameters/queryPage'
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, confirmed, delivered, cancelled]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Order'
                  meta:
                    $ref: '#/components/schemas/PaginatedMeta'
    post:
      tags: [Orders]
      summary: Passer une commande
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrderInput'
      responses:
        '201':
          description: Commande créée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /orders/all:
    get:
      tags: [Orders]
      summary: Toutes les commandes (admin)
      parameters:
        - $ref: '#/components/parameters/queryPage'
        - $ref: '#/components/parameters/queryPerPage'
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, confirmed, delivered, cancelled]
        - name: user_id
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Order'
                  meta:
                    $ref: '#/components/schemas/PaginatedMeta'
        '403':
          $ref: '#/components/responses/Forbidden'

  /orders/{id}:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Orders]
      summary: Détail d'une commande
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        '404':
          $ref: '#/components/responses/NotFound'

  /orders/{id}/status:
    parameters:
      - $ref: '#/components/parameters/pathId'
    put:
      tags: [Orders]
      summary: Changer le statut d'une commande (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrderStatusUpdate'
      responses:
        '200':
          description: Statut mis à jour
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        '403':
          $ref: '#/components/responses/Forbidden'

  /orders/{id}/cancel:
    parameters:
      - $ref: '#/components/parameters/pathId'
    post:
      tags: [Orders]
      summary: Annuler une commande (owner ou admin)
      responses:
        '200':
          description: Commande annulée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'

  /orders/{id}/delivery-cost:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Orders]
      summary: Frais de livraison d'une commande
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliverCost'
    post:
      tags: [Orders]
      summary: Renseigner les frais de livraison (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeliverCostInput'
      responses:
        '201':
          description: Créé
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeliverCost'

  # ═══════════════════════════
  # PAYMENTS
  # ═══════════════════════════
  /orders/{id}/payments:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Payments]
      summary: Paiements associés à une commande
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Payment'
    post:
      tags: [Payments]
      summary: Initier un paiement
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentInput'
      responses:
        '201':
          description: Paiement initié
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /payments/{id}:
    parameters:
      - $ref: '#/components/parameters/pathId'
    get:
      tags: [Payments]
      summary: Détail d'un paiement
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
        '404':
          $ref: '#/components/responses/NotFound'

  /payments/{id}/confirm:
    parameters:
      - $ref: '#/components/parameters/pathId'
    post:
      tags: [Payments]
      summary: Confirmer manuellement un paiement (admin)
      responses:
        '200':
          description: Confirmé
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'
        '403':
          $ref: '#/components/responses/Forbidden'

  /payments/{id}/refund:
    parameters:
      - $ref: '#/components/parameters/pathId'
    post:
      tags: [Payments]
      summary: Rembourser un paiement (admin)
      responses:
        '200':
          description: Remboursé
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Payment'

  /payments/webhook/{provider_code}:
    parameters:
      - name: provider_code
        in: path
        required: true
        schema:
          type: string
    post:
      tags: [Payments]
      summary: Webhook de confirmation de paiement (appelé par le prestataire)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Payload propre à chaque prestataire (Wave, Orange Money, etc.)
      responses:
        '200':
          description: Reçu
        '400':
          description: Signature invalide

  # ═══════════════════════════
  # PAYMENT METHODS
  # ═══════════════════════════
  /payment-methods:
    get:
      tags: [Payment Methods]
      summary: Liste des méthodes de paiement actives
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PaymentMethod'
    post:
      tags: [Payment Methods]
      summary: Créer une méthode de paiement (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentMethodInput'
      responses:
        '201':
          description: Créée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentMethod'

  /payment-methods/{provider_code}:
    parameters:
      - name: provider_code
        in: path
        required: true
        schema:
          type: string
    put:
      tags: [Payment Methods]
      summary: Modifier une méthode de paiement (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentMethodInput'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentMethod'
    delete:
      tags: [Payment Methods]
      summary: Supprimer une méthode de paiement (admin)
      responses:
        '204':
          description: Supprimée

  # ═══════════════════════════
  # CURRENCIES
  # ═══════════════════════════
  /currencies:
    get:
      tags: [Currencies]
      summary: Liste des devises supportées
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Currency'
    post:
      tags: [Currencies]
      summary: Ajouter une devise (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Currency'
      responses:
        '201':
          description: Créée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Currency'

  /currencies/{code_iso}:
    parameters:
      - name: code_iso
        in: path
        required: true
        schema:
          type: string
          maxLength: 3
    put:
      tags: [Currencies]
      summary: Modifier une devise (admin)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Currency'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Currency'
    delete:
      tags: [Currencies]
      summary: Supprimer une devise (admin)
      responses:
        '204':
          description: Supprimée

tags:
  - name: Auth
    description: Inscription, connexion, JWT
  - name: Users
    description: Gestion des comptes utilisateurs
  - name: Addresses
    description: Adresses de livraison
  - name: Categories
    description: Arborescence de catégories produits
  - name: Products
    description: Catalogue, images, prix, stock, specs, avis
  - name: Orders
    description: Commandes et livraisons
  - name: Payments
    description: Paiements, webhooks, remboursements
  - name: Payment Methods
    description: Méthodes de paiement (Wave, Orange Money, etc.)
  - name: Currencies
    description: Devises (MGA, EUR, USD…)
